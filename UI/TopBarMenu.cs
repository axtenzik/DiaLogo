using Godot;
using System;

[Tool]
public partial class TopBarMenu : Control
{
	/*[Signal]
    public delegate void AddDialogueEventHandler(PackedScene dialogueType);
    private PackedScene _dialogueType = GD.Load<PackedScene>("res://addons/DiaLogo/SimpleDialogue.tscn");
*/
	// Called when the node enters the scene tree for the first time.
	public override void _Ready()
	{
	}
}
